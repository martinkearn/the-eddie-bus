{
  "name": "the-eddie-bus",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n web,api -c blue,green \"npm:dev:web\" \"npm:dev:api\"",
    "dev:web": "next dev",
    "dev:api": "sh -c 'existing=$(lsof -ti tcp:8080); if [ -n \"$existing\" ]; then kill $existing; fi; BOOKING_API_CONFIG_FILE=\"$PWD/backend/config.private.php\" php -S 127.0.0.1:8080 -t backend/public'",
    "build": "next build",
    "start": "next start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "overrides": {
    "postcss": "8.5.10"
  }
}
